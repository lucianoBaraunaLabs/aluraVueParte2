export default class Foto {
    // Essa classe foi criada para ser um modelo dos dados da nossa foto dentro
    // do nosso sistema.
    constructor(titulo='', url='', descricao='') {

        this.titulo = titulo;
        this.url = url;
        this.descricao = descricao;
    }
}